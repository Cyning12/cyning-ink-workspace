# SPEC — 方案3：Neo4j 图数据库（可选）

## 定位

- **前置依赖**：**方案1** 的 `graph.json`；与 **方案2** 为增强/替换后端关系（二选一或 `USE_NEO4J` 切换——由需求方定一种）。
- **触发条件**（概念）：节点规模或分析类诉求（最短路径、中心性、社区等）超出内存方案舒适区时启用。
- **核心产出**：导入脚本 + Cypher 查询封装 + 运维边界（持久化、备份、CI 不强依赖）。

## 路径（阶段顺序）

1. **环境**：Neo4j 部署形态（Aura / Docker / 其他）与连接配置归属（不写具体密钥）。
2. **导入**：`graph.json` → 幂等 MERGE 节点与关系 → 与 schema 版本策略对齐。
3. **查询层**：与方案2 对齐的函数语义，内部走 Cypher；复杂分析查询单独列表（待补）。
4. **统一接口（可选）**：环境变量或工厂切换内存实现与 Neo4j 实现，保证上层只依赖稳定 API。
5. **CI 策略**：默认 job 不依赖 Neo4j；若有集成测试，单独 pipeline 或 nightly（需求方定）。

## 与方案1 / 2 的关系

- **不替代** 契约类校验；**不替代** `graph.json` 作为文本可 diff 的基线（除非团队改决策）。
- 与方案2：**并存**时须明确单一真值源（JSON 再生 → 再导入，或 DB 为源——二选一并写进本 SPEC 续篇）。

## 待需求方补充

- （空）驱动版本、Cypher 清单、`docker-compose` 是否纳入本仓库、与方案2 回退条件。
