# SPEC — 方案2：内存图查询（工具可调用）

## 定位

- **前置依赖**：**方案1** 已产出稳定 `graph.json`（路径与更新节奏与方案1 一致）；**闸口 A** 已归档（见 [改进方向.md](../../改进方向.md)）。
- **核心产出**：内存邻接结构 + 查询 API（及 CLI / MCP 之一或组合），供 Agent 确定性取「上下游 / 影响集」。

## 路径（阶段顺序）

1. **加载**：启动或冷启动时读入 `graph.json`，构建有向图（N/E 规模假设由需求方写进非功能需求）。
2. **查询面**：BFS/DFS 类算法封装（下游、上游、路径存在、多起点并集、可读描述等——接口表由需求方补）。
3. **调用面**：CLI（`execute_command`）与/或 MCP 注册；与 Cursor 配置边界写清。
4. **Harness 挂钩**：任务审计 / spec 审查等帽子模板中「必须调用图查询」的步骤位（具体模板文件名由需求方列）。
5. **降级**：未装 Neo4j 时本方案独立可用（与方案3 解耦）。
6. **阶段门闸**：交付后须完成 **闸口 B**（现状 / 方案1 / 方案2 对比实验）并形成可追溯文档，作为方案3 **R2** 立项输入。

## 与上下游的关系

- **上游**：仅依赖方案1 的 JSON；可选「直接读 `.ai.md`」为备选路径时须单独 SPEC 说明，避免双真值。
- **下游**：方案3 **仅**在闸口 B 与 **R2** 后启用；若与方案3 并存且结论冲突，**以 Neo4j（DB）为准**（**R3**，见 [改进方向.md](../../改进方向.md)）。

## 待需求方补充

- 模块名、命令行参数、MCP `mcp.json` 片段、与 `改进方向.md` 中 Prompt 模板的精确路径对齐。
- **闸口 B** 报告落盘路径（建议在 `query_graph/eval/` 或子仓 `docs/` 下由 task 定稿）。
