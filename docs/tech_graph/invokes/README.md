# tech_graph 专题 — Invoke 快照

> **用途**：本专题（graph.json / 三方案 / R1～R4）开 **需求帽 `10`**、执行帽等时，将 **已替换占位符后的可复制 Prompt 全文** 落盘于此，作为与 `../tasks/`、`../spec/`、`../SPEC/` 并列的可追溯锚点。  
> **与 Harness 通目录分工**：工作区通用 invoke 仍见 [`../../harness/invokes/README.md`](../../harness/invokes/README.md)；**凡绑定 `docs/tech_graph/` 规划与专题 SPEC 的调用体**，优先落 **本目录**，避免与跨仓 Harness 任务快照混放。

## 命名建议

与 Harness `invoke_YYYYMMDD_HHmm_10_<slug>.md` 习惯一致；`slug` 建议含 `tech-graph` 专题前缀。

## 文件正文结构

与 [`../../harness/invokes/README.md`](../../harness/invokes/README.md) **§4** 相同：元数据表 + `## 可复制 Prompt 快照` fenced 块。

## 给 Cursor

`tech_graph`、`invoke`、`快照`、`prompts`、`专题`
